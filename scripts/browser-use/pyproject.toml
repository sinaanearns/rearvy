[project]
name = "rearvy-browser-use-runner"
version = "0.1.0"
description = "Local browser-use runner for Rearvy Desktop development"
requires-python = ">=3.11"
dependencies = [
  "browser-use>=0.8.0",
  "python-dotenv>=1.0.1",
  "langchain-openai>=0.2.0",
  "langchain-google-genai>=2.0.0",
]

[tool.uv]
package = false
