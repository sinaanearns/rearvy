{
  "buildCommand": "npm install && cd website && npm install && cd .. && npm run build",
  "rewrites": [
    {
      "source": "/__/auth/(.*)",
      "destination": "https://rearvy-74c50.firebaseapp.com/__/auth/$1"
    }
  ]
}
