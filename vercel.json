{
  "buildCommand": "npm install && npm install --include=dev --prefix=website && npm run build",
  "rewrites": [
    {
      "source": "/__/auth/(.*)",
      "destination": "https://rearvy-74c50.firebaseapp.com/__/auth/$1"
    }
  ]
}
