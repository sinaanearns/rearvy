{
  "installCommand": "npm install --include=dev --prefix=website --no-audit --progress=false",
  "buildCommand": "npm run build --prefix=website",
  "outputDirectory": "website/.next",
  "rewrites": [
    {
      "source": "/__/auth/(.*)",
      "destination": "https://rearvy-74c50.firebaseapp.com/__/auth/$1"
    }
  ]
}
