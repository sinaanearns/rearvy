{
  "buildCommand": "npm install && npm install --include=dev --prefix=website && cd website && npm run build",
  "outputDirectory": "website/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/__/auth/(.*)",
      "destination": "https://rearvy-74c50.firebaseapp.com/__/auth/$1"
    }
  ]
}
